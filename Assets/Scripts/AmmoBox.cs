using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmmoBox : MonoBehaviour
{
    public int ammo, life;
    // Start is called before the first frame update
    void Start()
    {
        ammo = 10;
        life = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
